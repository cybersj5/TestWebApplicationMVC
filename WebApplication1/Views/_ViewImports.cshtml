@using WebApplication1
@using WebApplication1.Models
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@*Функции этого файла: Импорт ппространства имён и Добавление тег-хелперов.
Префикс с нижним подчёркиванием (_) в имени файла указывает на то, что это частичный файл (partial file). 
Это означает, что этот файл не обрабатывается напрямую как страница или представление.*@